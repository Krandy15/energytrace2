# Copy this file to `.env` and adjust as needed.
# Only used once the real backend is running (see /backend) — the app
# currently reads local JSON files and does not call this URL yet.
VITE_API_URL=http://127.0.0.1:8000
